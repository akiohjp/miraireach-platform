# mirAIreach 漫画画像 — 差し替え用フォルダ

このフォルダ（`Portal/public/miraireach/`）に **そのまま上書き** してください。  
ファイル名を変えないでください。

## 入れるファイル（3枚）

| ファイル名 | 内容 |
|-----------|------|
| `manga-01.jpg` | 1枚目（カフェ・AI Overview の話） |
| `manga-02.jpg` | 2枚目（AIO・4つの機能） |
| `manga-03.jpg` | 3枚目（100+媒体・3ヶ月後） |

## 推奨スペック（くっきり表示するため）

- **形式:** JPEG（`.jpg`）
- **幅:** **1440px 以上**（推奨 1800px）
- **品質:** 85〜92%
- **縦横比:** 3枚とも同じ比率で OK（例: 縦長 2:3 前後）

> 幅 721px のままだと、Retina 画面で大きく表示したときにぼやけます。

## 反映のしかた

1. 上記3ファイルをこのフォルダに保存（既存ファイルを上書き）
2. ローカル: `Portal` で `npm run dev` → http://localhost:3000#miraireach-system  
   ブラウザは **Ctrl+Shift+R**（スーパーリロード）
3. 本番: 「本番に反映して」と依頼 → Vercel にデプロイ

## 注意

- `.png` ではなく **`.jpg`** で保存（コードは `.jpg` を参照）
- 古い `manga-01.png` などは使われません（削除して問題ありません）
