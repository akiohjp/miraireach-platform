# mirAIreach 漫画画像 — 差し替え用フォルダ

このフォルダ（`Portal/public/miraireach/`）に **そのまま上書き** してください。  
ファイル名を変えないでください。

## 入れるファイル（3枚）

| ファイル名 | 内容 |
|-----------|------|
| `manga-01.png` | 1枚目（カフェ・AI Overview の話） |
| `manga-02.png` | 2枚目（AIO・4つの機能） |
| `manga-03.png` | 3枚目（100+媒体・3ヶ月後） |

※ `mirAI英語_001.png` など別名で入れた場合は、上記ファイル名にリネームするか、開発側で取り込みます。

## 推奨スペック（くっきり表示するため）

- **形式:** PNG または JPEG
- **幅:** **1440px 以上**（現在の採用サイズ: 2039×2894）
- **縦横比:** 3枚とも同じ比率で OK

## 反映のしかた

1. 上記3ファイルをこのフォルダに保存（既存ファイルを上書き）
2. ローカル: `Portal` で `npm run dev` → http://localhost:3000#miraireach-system  
   ブラウザは **Ctrl+Shift+R**（スーパーリロード）
3. 本番: 「本番に反映して」と依頼 → Vercel にデプロイ

## 注意

- コードは **`manga-01.png`〜`03.png`** を参照（`.jpg` でも可。パスを合わせること）
- 古い低解像度ファイルは上書きで置き換え
